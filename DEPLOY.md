# מדריך העלאה לאירוח — מהנייד בלבד, חינם

מדריך זה מלווה אותך שלב־שלב בהעלאת האפליקציה לשרת מקוון **ישירות מהאנדרואיד**,
בלי מחשב ובלי טרמינל — הכל דרך הדפדפן. התוצאה: קישור שתוכל לשלוח למפקדים,
וכל מי שנכנס אליו רואה ומעדכן את אותם נתונים בזמן אמת (סנכרון מלא).

העלות: **0 ₪**. משתמשים בשני שירותים חינמיים:
1. **Neon** — בסיס הנתונים (PostgreSQL) שבו נשמר המידע לתמיד.
2. **Render** — השרת שמריץ את האפליקציה.

> טיפ: פתח את המדריך במסך אחד ואת הדפדפן במסך שני (או עבור ביניהם).
> תצטרך להעתיק פעם אחת מחרוזת טקסט מ-Neon ל-Render.

---

## חלק א׳ — יצירת בסיס הנתונים (Neon)

1. היכנס מהדפדפן לכתובת **https://neon.tech** ולחץ **Sign up**
   (אפשר להירשם עם חשבון Google או GitHub — הכי מהיר).
2. צור פרויקט חדש (**Create project**):
   - שם: משהו כמו `mahlaka-1`.
   - אזור (Region): בחר **Europe (Frankfurt)** — הכי קרוב לישראל.
   - שאר ההגדרות — השאר כברירת מחדל ולחץ **Create**.
3. בסיום, Neon יציג מסך עם **Connection string** (מחרוזת חיבור) שנראית כך:
   ```
   postgresql://user:password@ep-xxxx.eu-central-1.aws.neon.tech/dbname?sslmode=require
   ```
   לחץ על כפתור ההעתקה (Copy) ושמור אותה זמנית (למשל הדבק לעצמך בהודעה
   בוואטסאפ). **נשתמש בה בחלק ב׳.**

> אם המסך נסגר: בפרויקט ב-Neon יש כפתור **Connect** / **Connection Details**
> שמציג שוב את אותה מחרוזת.

---

## חלק ב׳ — העלאת האפליקציה (Render)

1. היכנס מהדפדפן לכתובת **https://render.com** ולחץ **Get Started** /
   **Sign up**, והירשם **עם GitHub** (החשבון שבו נמצא הקוד).
2. אשר ל-Render גישה למאגר **`Nmishaly/Department-1-exits`**
   (אפשר לתת גישה רק למאגר הזה).
3. בלוח הבקרה לחץ על **New +** ובחר **Blueprint**.
4. בחר את המאגר `Department-1-exits`. Render יזהה אוטומטית את קובץ
   ההגדרות `render.yaml` שכבר נמצא בקוד.
5. Render יבקש למלא שני ערכים (השאר את האחרים כפי שהם):
   - **APP_PASSWORD** — הסיסמה שהמפקדים יזינו בכניסה. בחר סיסמה טובה.
   - **DATABASE_URL** — הדבק כאן את מחרוזת החיבור שהעתקת מ-Neon בחלק א׳.
   - (המפתח `SESSION_SECRET` נוצר אוטומטית — לא צריך לגעת.)
6. לחץ **Apply** / **Create**. Render יתחיל לבנות ולהעלות (לוקח כ-1–3 דקות).
7. בסיום יופיע קישור כמו:
   ```
   https://yetziot-mahlaka-1.onrender.com
   ```
   (ייתכן שם מעט שונה). **זו הכתובת שתשלח למפקדים** יחד עם הסיסמה.

זהו! פתח את הקישור בנייד, התחבר עם הסיסמה, וכל הנתונים כבר שם.

---

## שאלות נפוצות ותחזוקה

**הטעינה הראשונה איטית / "נתקע" לכמה שניות.**
בגרסה החינמית של Render השרת "נרדם" אחרי 15 דקות ללא שימוש, ומתעורר בכניסה
הבאה. לכן הכניסה הראשונה אחרי הפסקה עלולה לקחת ~30–50 שניות. אחרי זה מהיר.

**איך משנים את הסיסמה של המפקדים?**
ב-Render: נכנסים לשירות ← לשונית **Environment** ← עורכים את `APP_PASSWORD`
← **Save**. השרת יתעדכן אוטומטית.

**איך מעלים עדכון לקוד?**
כל שינוי שנדחף ל-branch שהוגדר (ב-`render.yaml`) גורם ל-Render לפרוס מחדש
אוטומטית. אין צורך לעשות דבר.

**האם המידע נשמר?**
כן. המידע נשמר ב-Neon (בסיס הנתונים) ולא נמחק בעדכונים או בהפעלות מחדש.

**רוצה לגבות את הנתונים?**
ב-Neon יש אפשרות **Backups** / ייצוא. אפשר גם להשתמש בכלי חיצוני, אבל
לרוב אין צורך — Neon שומר גיבויים בעצמו.

> אבטחה: הסיסמה והמפתח הסודי מוגדרים רק בממשק של Render (משתני סביבה),
> ואף פעם לא נשמרים בקוד או ב-GitHub.
