# מדריך העלאה לאירוח (Fly.io)

מדריך זה מלווה אותך שלב־שלב בהעלאת האפליקציה לשרת מקוון ב-Fly.io, כך שתוכל
לגשת אליה מהנייד ולשלוח למפקדים קישור. כל המפקדים שייכנסו לאותו קישור יראו
וישתפו את אותם נתונים בזמן אמת (סנכרון מלא בין המכשירים).

> את הפקודות מריצים במחשב (Mac / Windows / Linux) עם חיבור לאינטרנט.
> אני לא יכול לבצע את ההעלאה עבורך כי היא דורשת חשבון וסיסמאות שלך — אבל
> הכל כבר מוכן בקוד, ופשוט צריך לעקוב אחרי השלבים.

---

## שלב 1 — התקנת הכלי של Fly (flyctl)

**Mac / Linux:**
```bash
curl -L https://fly.io/install.sh | sh
```

**Windows (PowerShell):**
```powershell
pwsh -Command "iwr https://fly.io/install.sh -useb | iex"
```

לאחר ההתקנה, אולי תצטרך לפתוח חלון טרמינל חדש כדי שהפקודה `fly` תזוהה.

## שלב 2 — פתיחת חשבון / התחברות

```bash
fly auth signup      # לפתיחת חשבון חדש
# או, אם כבר יש לך חשבון:
fly auth login
```
(ידרוש הזנת פרטי תשלום — Fly דורש כרטיס אשראי לאימות, גם בשימוש הזול/החינמי.)

## שלב 3 — הורדת הקוד למחשב

```bash
git clone https://github.com/Nmishaly/Department-1-exits.git
cd Department-1-exits
git checkout claude/soldier-leave-tracking-app-3mgwy3
```
(אם כבר מיזגת את הקוד ל-main, אפשר לדלג על שורת ה-`checkout`.)

## שלב 4 — יצירת האפליקציה ב-Fly

שם האפליקציה חייב להיות ייחודי בכל Fly (הוא יהפוך לחלק מהכתובת).
בחר שם משלך, למשל `yetziot-mahlaka-1-2026`:

```bash
fly launch --no-deploy --copy-config --name yetziot-mahlaka-1-2026 --region fra
```

- `--copy-config` — משתמש בהגדרות המוכנות שבקובץ `fly.toml`.
- `--no-deploy` — לא מעלה עדיין, קודם נשלים את ההגדרות.
- `--region fra` — פרנקפורט (קרוב יחסית לישראל). אפשר לבחור אזור אחר.
- אם הכלי שואל אם להוסיף בסיס נתונים (Postgres/Redis) — ענה **לא**.

## שלב 5 — יצירת כונן קבוע לנתונים

זה מה ששומר את בסיס הנתונים (SQLite) גם אחרי עדכונים והפעלות מחדש:

```bash
fly volumes create data --region fra --size 1 --yes
```
> חשוב: השם חייב להיות `data` והאזור חייב להיות זהה לזה שבחרת בשלב 4.

## שלב 6 — הגדרת הסיסמה והמפתח הסודי

בחר סיסמה למפקדים, וצור מפתח סודי אקראי לחתימת ההתחברות:

```bash
# יצירת מפתח אקראי (העתק את הפלט):
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# אם אין Node במחשב, אפשר גם: openssl rand -hex 32
```

ואז הגדר את שני הסודות (החלף את הערכים בשלך):

```bash
fly secrets set APP_PASSWORD='הסיסמה-שבחרת' SESSION_SECRET='המפתח-האקראי-שהעתקת'
```

## שלב 7 — העלאה!

```bash
fly deploy
```

לאחר שהעלאה מסתיימת, ודא שיש מכונה אחת בלבד (חשוב לבסיס נתונים SQLite):
```bash
fly scale count 1
```

## שלב 8 — פתיחה ושיתוף

```bash
fly open
```
זה יפתח את האפליקציה בכתובת `https://yetziot-mahlaka-1-2026.fly.dev`
(עם השם שבחרת). זו הכתובת שתשלח למפקדים יחד עם הסיסמה.

---

## פעולות שימושיות בהמשך

| מה רוצים לעשות            | פקודה                                             |
|--------------------------|--------------------------------------------------|
| לשנות את סיסמת המפקדים    | `fly secrets set APP_PASSWORD='סיסמה-חדשה'`       |
| לראות לוגים / תקלות       | `fly logs`                                        |
| להעלות עדכון לקוד         | `git pull` ואז `fly deploy`                       |
| לגבות את בסיס הנתונים     | `fly ssh sftp get /data/data.db ./backup.db`     |
| לראות סטטוס               | `fly status`                                      |

### הערות
- האפליקציה מוגדרת "להירדם" כשאין שימוש ולהתעורר אוטומטית בכניסה הבאה
  (חיסכון בעלויות). לכן ייתכן שהטעינה הראשונה אחרי חוסר פעילות תיקח
  כמה שניות — זה תקין.
- הנתונים נשמרים על הכונן הקבוע (`/data`) וממשיכים להתקיים גם אחרי כל
  `fly deploy`.
- כל עוד כולם ניגשים לאותה כתובת — כל המפקדים רואים את אותם הנתונים,
  והסנכרון בין המכשירים הוא מיידי.
- **חשוב:** אל תשמור את הסיסמה או המפתח הסודי בתוך הקוד או ב-git. הם
  מוגדרים רק דרך `fly secrets`.
